%h1= @wishlist.name
%h5= @wishlist.description

%br

%ul
	- @wishlist.items.each do |item|
		%li= item.name
		%br= item.description
		%br Price
		= item.price
		%br
		%br Seller's positive feedback percentage
		= item.feedback_score_percent
		%br

%br
= link_to "Add new item to list", new_wishlist_item_path(wishlist_id: @wishlist.id)